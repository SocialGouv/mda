global:
  host: maisondelautisme.fabrique.social.gouv.fr
  # host: maisondelautisme.gouv.fr #TODO enable when domain is ok

jobs:
  runs:
    build-web:
      with:
        buildArgs:
          PRODUCTION: "true"
          ROBOT: "true"
