# global:
#   host: maisonautisme.gouv.fr #TODO enable when domain is ok

web:
  vars:
    MDA_ENV: prod

jobs:
  runs:
    build-web:
      with:
        buildArgs:
          PRODUCTION: "true"
          ROBOT: "true"
